# Heimdall SDK (Node.js)

Register a simple `/__ping__` route in your Express.js server to be pinged by monitoring platforms and prevent cold starts.

## Installation

```bash
npm install heimdall-sdk
